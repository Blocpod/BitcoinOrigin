// Synthetic fixture used to demonstrate ORIGIN's lexical signal scanner.
// Matches are not conclusions.
static const char* checkpoint_label = "trusted checkpoint fixture";
void local_operator_tool() {
  // invalidateblock is a local administrative RPC in several node families.
}
